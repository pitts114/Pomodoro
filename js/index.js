/*

$(document).ready(function() {
    var workTimer = document.createElement("p");
    var node = document.createTextNode("10:00");
    workTimer.appendChild(node);
    var timerCircle = document.getElementById("circle");
    timerCircle.appendChild(workTimer);
});
*/